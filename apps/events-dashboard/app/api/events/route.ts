import {NextRequest,NextResponse} from "next/server";import {requireActor} from "@/lib/auth";import {repository} from "@/lib/repository";import {EventService} from "@/lib/service";import {CreateRequest} from "@/lib/schema";import {errorResponse} from "@/lib/api";import type{EventRecord}from"@/lib/types";import {recordDataAccess,setDataTraceOutcome,withDataTrace} from "@fika/server-shared/data-source-meter-server";
export const runtime="nodejs";export const dynamic="force-dynamic";export async function GET(req:NextRequest){return withDataTrace({app:"events-dashboard",action:"event.list",path:"/api/events",dataset:"events-dashboard/events",outcome:"SUCCESS"},async()=>{try{const actor=requireActor(req);const records=new EventService(repository()).list();recordDataAccess({operation:"event.list",source:"UNKNOWN",documents:records.length,dataset:"events-dashboard/events"});return NextResponse.json({records,actor})}catch(e){setDataTraceOutcome((e as {status?:number}).status===401?"DENIED":"ERROR");return errorResponse(e)}})}export async function POST(req:NextRequest){return withDataTrace({app:"events-dashboard",action:"event.create",path:"/api/events",dataset:"events-dashboard/events",outcome:"SUCCESS"},async()=>{try{const actor=requireActor(req);const body=CreateRequest.parse(await req.json());const result=new EventService(repository()).create(body.requestId,body.event as Partial<EventRecord>,actor.id);recordDataAccess({operation:"event.create",source:"UNKNOWN",documents:0,dataset:"events-dashboard/events"});return NextResponse.json(result,{status:201})}catch(e){setDataTraceOutcome((e as {status?:number}).status===401?"DENIED":"ERROR");return errorResponse(e)}})}
