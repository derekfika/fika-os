import {NextRequest,NextResponse} from "next/server";import {requireActor} from "@/lib/auth";import {repository} from "@/lib/repository";import {EventService} from "@/lib/service";import {CreateRequest} from "@/lib/schema";import {errorResponse} from "@/lib/api";import type{EventRecord}from"@/lib/types";
export const runtime="nodejs";export const dynamic="force-dynamic";export async function GET(req:NextRequest){try{const actor=requireActor(req);return NextResponse.json({records:new EventService(repository()).list(),actor})}catch(e){return errorResponse(e)}}export async function POST(req:NextRequest){try{const actor=requireActor(req);const body=CreateRequest.parse(await req.json());return NextResponse.json(new EventService(repository()).create(body.requestId,body.event as Partial<EventRecord>,actor.id),{status:201})}catch(e){return errorResponse(e)}}
