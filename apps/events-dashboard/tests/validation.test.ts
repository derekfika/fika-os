import test from"node:test";import assert from"node:assert/strict";import{CreateRequest}from"../lib/schema.ts";import{requireActor}from"../lib/auth.ts";const event={eventName:"",eventType:"",description:"",eventDate:"",startTime:"",endTime:"",timezone:"Europe/London",pax:null,responsibleOplocId:"",siteId:"",eventContact:"",accountableOwnerId:"",contributorIds:[],lifecycleStatus:"Draft",staffingRequirements:[],productionRequirements:[],tasks:[]};test("strict request rejects unknown fields",()=>assert.equal(CreateRequest.safeParse({requestId:"request:123",event,unknown:true}).success,false));test("malformed request rejected",()=>assert.equal(CreateRequest.safeParse("bad").success,false));test("development identity authorises locally",()=>{const actor=requireActor({headers:new Headers()} as never);assert.equal(actor.development,true)});test("explicit authenticated identity is accepted",()=>{const actor=requireActor({headers:new Headers({"x-fika-user-id":"person:test","x-fika-user-name":"Test User"})} as never);assert.equal(actor.id,"person:test")});
test("production rejects an unauthenticated write",()=>assert.throws(()=>requireActor({headers:new Headers()} as never,"production"),/Authentication required/));
