"use client";
import { useEffect, useState } from "react";
import type { CatalogueEntry } from "@/lib/catalogue";

type Response = { entries: CatalogueEntry[]; total: number; filteredCount: number; categories: string[] };
const human = (value: string) => value.replaceAll("_", " ").replace(/\b\w/g, (letter) => letter.toUpperCase());

export default function CatalogueWorkspace() {
  const [data, setData] = useState<Response>({ entries: [], total: 0, filteredCount: 0, categories: [] });
  const [query, setQuery] = useState(""); const [category, setCategory] = useState("all"); const [usage, setUsage] = useState("all"); const [status, setStatus] = useState("all");
  const [selected, setSelected] = useState<CatalogueEntry>(); const [loading, setLoading] = useState(true); const [error, setError] = useState("");
  useEffect(() => { const controller = new AbortController(); setLoading(true); const params = new URLSearchParams(); if (query) params.set("q", query); if (category !== "all") params.set("category", category); if (usage !== "all") params.set("usage", usage); if (status !== "all") params.set("status", status); fetch(`/api/catalogue?${params}`, { cache: "no-store", signal: controller.signal }).then(async (response) => { const body = await response.json() as Response & { error?: string }; if (!response.ok) throw new Error(body.error || "The catalogue could not be loaded."); setData(body); setError(""); }).catch((reason: Error) => { if (reason.name !== "AbortError") setError(reason.message); }).finally(() => setLoading(false)); return () => controller.abort(); }, [query, category, usage, status]);
  return <main className="menu-app"><header className="os-header"><a className="os-brand" href="/" aria-label="Back to Menu Planning"><strong>FIKA</strong><span>OS</span></a><div><small>Governed operations</small><h1>Menu Planning</h1></div><div className="os-header-actions"><a className="button button-mint" href="/dishes/new">Create draft dish</a></div></header><nav className="menu-nav" aria-label="Menu Planning"><a className="active" href="/catalogue">Catalogue</a><a href="/?view=week">Week plan</a><a href="/sandwiches">Saved sandwiches</a><a href="/sources">Source review</a></nav><section className="menu-page-heading catalogue-hero"><div><small>Canonical MenuItems only</small><h2>Everything your chefs make.</h2><p>Only explicitly promoted records appear here. Source material stays in review and nothing is auto-approved.</p></div><div className="catalogue-stats"><strong>{data.filteredCount}</strong><span>of {data.total} canonical items shown</span><small>Review source material separately.</small></div></section><section className="workspace-panel catalogue-workspace"><div className="catalogue-toolbar"><label>Search the catalogue<input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Name, description or source" /></label><label>Category<select value={category} onChange={(event) => setCategory(event.target.value)}><option value="all">All categories</option>{data.categories.map((value) => <option key={value} value={value}>{human(value)}</option>)}</select></label><label>Used for<select value={usage} onChange={(event) => setUsage(event.target.value)}><option value="all">All uses</option><option value="hospitality">Hospitality</option><option value="delivered-in">Delivered-in lunch</option><option value="production">Production</option></select></label><label>Status<select value={status} onChange={(event) => setStatus(event.target.value)}><option value="all">All statuses</option><option value="draft">Draft</option><option value="approved">Approved</option><option value="archived">Archived</option></select></label></div>{error && <div className="menu-error" role="alert">{error}</div>}{loading ? <div className="empty-state"><span className="loader" /><p>Loading the catalogue...</p></div> : data.entries.length ? <div className="catalogue-grid">{data.entries.map((entry) => <button className="catalogue-card catalogue-card--canonical" key={entry.id} onClick={() => setSelected(entry)}><div className="catalogue-card-top"><span className="catalogue-kind">Canonical menu item</span><span className={`status status--${entry.status.replaceAll(" ", "_")}`}>{human(entry.status)}</span></div><h3>{entry.name}</h3><p>{entry.description || "No description recorded yet."}</p><div className="catalogue-meta"><span>{human(entry.category)}</span><span>{entry.usage.map(human).join(" · ")}</span></div><small>{entry.sourceLabel} · {entry.recipeAvailable ? "Recipe available" : "Recipe not recorded"}</small></button>)}</div> : <div className="empty-state"><h3>No canonical menu items yet.</h3><p>Promote a reviewed source candidate or create a draft dish deliberately.</p><a className="button button-purple" href="/sources">Review source candidates</a><a className="button button-soft" href="/dishes/new">Create a draft dish</a></div>}</section>{selected && <div className="modal-backdrop" role="presentation" onMouseDown={(event) => { if (event.target === event.currentTarget) setSelected(undefined); }}><aside className="item-detail" role="dialog" aria-modal="true"><header><div><small>Canonical menu item · {human(selected.status)}</small><h3>{selected.name}</h3></div><button className="icon-button" onClick={() => setSelected(undefined)} aria-label="Close detail">×</button></header><section><a className="button button-purple" href="/dishes/new">Open editor</a><a className="button button-soft" href="/?view=week">Use in week plan</a></section><dl><dt>Category</dt><dd>{human(selected.category)}{selected.subcategory ? ` · ${human(selected.subcategory)}` : ""}</dd><dt>Source</dt><dd>{selected.sourceLabel}</dd><dt>Stable ID</dt><dd>{selected.id}</dd><dt>Allergen evidence</dt><dd>{selected.allergenCount ? `${selected.allergenCount} decision(s)` : "No decisions recorded"}</dd></dl></aside></div>}</main>;
}
