// Keep the shared safety-contract tests in the CPU test command as well as
// the package-level suite: this contract is consumed at the CPU signing seam.
import "../../../packages/server-shared/tests/daily-signed-oploc-bundle.test";
