rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // The trusted Next.js server is the only application writer. The emulator
    // Admin SDK bypasses these rules; browsers have no direct database access.
    match /{document=**} { allow read, write: if false; }
  }
}
