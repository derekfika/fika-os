export const metadata = { title: "Munich Re Hospitality", description: "Munich Re hospitality booking and operations" };

export default function MunichReLayout({ children }: { children: React.ReactNode }) {
  return children;
}
